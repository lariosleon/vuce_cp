import { Routes } from '@angular/router';
// Definición de una constante `routes` que es un array de objetos de tipo Routes.
// Actualmente, el array está vacío, pero está preparado para definir rutas en la aplicación Angular.
export const routes: Routes = [];
